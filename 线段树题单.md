# 🌳 线段树练习计划（从入门到进阶）

> 线段树是「**单点/区间修改 + 区间查询**」的利器，也是竞赛常客。
> 配套模板：`板子/数据结构/线段树.cpp`（含懒标记）。每做完一题把 `.cpp` 放进 `洛谷/数据结构/`（或新建分类），`- [ ]` 勾掉。
> 做完一题建议更新 README 统计（跑 `node gen-readme.js`）。

---

## 0. 先备（建议先会）

- 数组 / 递归 / 二分思想
- 前缀和与差分（`板子/杂项/前缀和与差分.cpp`）
- （可选）树状数组（`板子/数据结构/树状数组.cpp`）——先感受「单点改 + 区间查」的轻量版

> ⚠️ 说实话：线段树基础对 CF 1058 略超前（约 1400+ 内容）。**别急，先把第 1 关模板彻底吃透，再往后走**。第 0 关没打好地基，后面会越刷越懵。

---

## 1. 模板关（必做）

- [ ] **P3372 【模板】线段树 1** —— 区间加 + 区间求和（核心，带懒标记）
- [ ] 把 `板子/数据结构/线段树.cpp` 的三个操作（`build` / `update` / `query`）**手写一遍**，理解：
  - 为什么数组要开 **4 倍**空间（`4*n` vs `2*n`）
  - **懒标记（lazy）**为什么能省时间、以及**下传**（pushdown）的时机
  - 递归的「区间划分」逻辑（`mid=(l+r)/2`）

## 2. 单点改 + 区间查（入门应用）

- [ ] **P1198 [JSOI2008] 最大数** —— 单点加 + 区间最大（简单，练手）
- [ ] **P1531 I Hate It** —— 单点改 + 区间最大
- [ ] **P3374 【模板】树状数组 1** —— 对比学习（用树状数组实现同款「单点改+区间和」，更简单，体会两者区别）

## 3. 稍微变形（进阶一段）

- [ ] **P4513 小白逛公园** —— 区间**最大子段和**（要点：每个节点维护 `lmax/rMax/sum/ans` 4 个值，合并时更新）★ 很练人
- [ ] **P4145 上帝造题的七分钟2** —— 区间开方（每个数开方次数有限 → 暴力改 + 剪枝，不无脑推懒标记）
- [ ] **P1438 无聊的数列** —— 区间加等差 + 区间和（差分思想套线段树）

## 4. 高强度（可选，1600+ 再冲）

- [ ] **P3373 【模板】线段树 2** —— 区间加 + 区间乘（**双懒标记**，注意乘法下传顺序）★
- [ ] **P5490 【模板】扫描线** —— 线段树 + 扫描线求矩形面积并（经典硬题）
- [ ] **P3834 【模板】可持久化线段树 1（主席树）** —— 区间第 k 小（前置：线段树 + 前缀和思想）

## 5. 高阶（先别碰，2000+）

- 线段树合并、动态开点、树链剖分、李超树、吉老师线段树（区间取 min/max）

---

## 做题方法（重要）

1. 每做一题，先**用自己理解把 4 个核心操作写出来**（`build` / `update` / `query` / `pushdown`），再动码。
2. 卡住就看 `板子/数据结构/线段树.cpp`，但**一定要能默写一遍**——考试时模板是默出来的，不是抄出来的。
3. **线段树最易错**：
   - `4*n` 数组开小 → RE
   - `pushdown` 忘下传 / 下传顺序错（尤其 P3373 加乘）
   - `int` 溢出 → `long long`（区间和/乘）
   - 边界 `l==r` 的判断、递归时 `l,r` 传错

---

## 进度

- 第 1 关到第 3 关刷完，线段树基本就稳了；之后把它当"工具箱"随刷题随用。
