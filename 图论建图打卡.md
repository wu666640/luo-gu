# 🕸️ 图论建图打卡

> **用法**：做每题前先自己填「我的四问」（顶点/连边/边权/要求），**能填出来 = 真会建图**。
> 卡住了再看「参考答案」，然后把这题的 `.cpp` 放进 `洛谷` 对应分类文件夹。
> 每做一题，把「四问」从填空改成你填的内容并 `- [ ]` 勾掉。
> 配套：`ACM学习/图论学习与建图.md`（四问 + 8 招技巧详解）。

---

## 第 1 步：存图 + 遍历

### P3916 图的遍历
- [ ] 考点：反图 + DFS
- **我的四问**：
  - 顶点（谁是点）：＿＿＿＿＿
  - 连边（何时连/方向）：＿＿＿＿＿
  - 边权（是啥）：＿＿＿＿＿
  - 要求（算什么）：＿＿＿＿＿
- **参考答案**：① 顶点 = 1..N 每个点；② 连边 = 题目给的有向边，为了求"每点能到的最大编号"，建**反图**；③ 边权 = 无（只判可达，不用权）；④ 要求 = 每个点出发能到达的最大编号。建图：反向存边，从 N 到 1 依次 DFS 反图，标记 `ans`。

### P5318 图的遍历（✅ 已完成）
- [x] 考点：DFS/BFS 基础 + 排序
- **我的四问**：
  - 顶点 = 点 1..n
  - 连边 = 有向边 u→v；输出顺序要“编号小的先访问” → 每个点的出边**升序排序**
  - 边权 = 无（只要遍历顺序，不需要权）
  - 要求 = 从 1 出发，输出 DFS 序列 + BFS 序列（每点只访问一次）
- **参考答案**：① 顶点 = 1..N 点；② 边 = 有向边；③ 边权 = 无；④ 要求 = 按 DFS/BFS 顺序输出遍历序列（要求编号从小到大 → 邻接表**排序**）。建图：邻接表存边，对每个点的出边排序。

### P1141 01迷宫
- [ ] 考点：连通块（相邻"不同"连边）
- **我的四问**：＿＿＿＿
- **参考答案**：① 顶点 = 每个格子 `(x,y)`；② 边 = 上下左右相邻且**数字不同**的两个格子连边（无向）；③ 边权 = 无；④ 要求 = 每个格子所在连通块的大小。建图/BFS：遍历找连通块并记录大小。

---

## 第 2 步：最短路

### P3371 / P4779 单源最短路径
- [ ] 考点：Dijkstra（弱化/标准）
- **我的四问**：＿＿＿＿
- **参考答案**：① 顶点 = 点 1..n；② 边 = 有向带权边；③ 边权 = 两点距离/代价；④ 要求 = 从 1 号到每个点的最短路。建图：vector 邻接表 + Dijkstra（P4779 用堆优化）。

### P1629 邮递员送信
- [ ] 考点：反向建图
- **我的四问**：＿＿＿＿
- **参考答案**：① 顶点 = 邮局 1 + 各点；② 边 = 有向；③ 边权 = 时间；④ 要求 = 1 号到所有点再回到 1 的总最短时间。建图：**正图 + 反图各跑一次最短路**，相加。

### P1144 最短路计数
- [ ] 考点：无权最短路 + 计数 DP
- **我的四问**：＿＿＿＿
- **参考答案**：① 顶点 = 点；② 边 = 无向无权；③ 边权 = 1（每步）；④ 要求 = 1 号到每点最短路的**条数**。建图：无权图直接 BFS（分层），用 `cnt[v] += cnt[u]` 计数。

### P2296 寻找道路
- [ ] 考点：反向建图 + 可达性
- **我的四问**：＿＿＿＿
- **参考答案**：① 顶点 = 点；② 边 = 有向；③ 边权 = 无；④ 要求 = 从 s 到 t，且路径上每个点的**出边指向的点都能到 t** 的最短路。建图：先**反图**从 t 标记"能到 t 的点"，再据此筛出"可直接走"的点，跑最短路/BFS。

---

## 第 3 步：最小生成树

### P3366 最小生成树
- [ ] 考点：Kruskal
- **我的四问**：＿＿＿＿
- **参考答案**：① 顶点 = 点；② 边 = 无向带权；③ 边权 = 长度/代价；④ 要求 = 连接所有点的最小总权。建图：边集存所有边，排序后 Kruskal + 并查集。

### P1195 口袋的天空
- [ ] 考点：Kruskal（连成 k 块）
- **我的四问**：＿＿＿＿
- **参考答案**：① 顶点 = n 个点；② 边 = 无向带权；③ 边权 = 代价；④ 要求 = 连成 **k 个连通块**的最小代价。建图：Kruskal 连到连通块数 = k 时停止。

### P1991 无线通讯网
- [ ] 考点：Kruskal + 边数转换
- **我的四问**：＿＿＿＿
- **参考答案**：① 顶点 = p 个哨所；② 边 = 各哨所之间（无向带权=距离）；③ 边权 = 距离；④ 要求 = 用 s 个卫星（免费通信）+ 若干电话线，使全部连通时**最大的电话线长度最小**。建图：Kruskal 连到剩 **s 个连通块**时的那条边长度。

---

## 第 4 步：拓扑 / DAG

### P1113 杂务
- [ ] 考点：拓扑 + 点权最长路
- **我的四问**：＿＿＿＿
- **参考答案**：① 顶点 = 每项任务；② 边 = 依赖关系（先做的 → 后做的）；③ 边权 = 点权（任务耗时）；④ 要求 = 全部完成的最早时间。建图：拓扑排序，`dp[v]=max(dp[v], dp[u]+cost[v])`。

### P4017 最大食物链计数
- [ ] 考点：DAG 拓扑 DP（计数）
- **我的四问**：＿＿＿＿
- **参考答案**：① 顶点 = 生物；② 边 = 捕食关系（生产者 → 消费者，有向）；③ 边权 = 无；④ 要求 = 从生产者到顶级消费者的**食物链条数**。建图：入度 0 为起点，拓扑 DP 累加。

### P1983 车站分级
- [ ] 考点：复杂建图 + 拓扑最长路（较难）
- **我的四问**：＿＿＿＿
- **参考答案**：① 顶点 = 车站；② 边 = 一趟车停靠的站，**未停靠站等级更低** → 未停靠的连向停靠的（有向）；③ 边权 = 无（等级差）；④ 要求 = 最少等级数。建图：对每趟车，把未停靠站连向所有停靠站（可用**虚点**优化），拓扑求最长路。

---

## 第 5 步：连通性 / 缩点

### P3367 并查集
- [ ] 考点：并查集
- **我的四问**：＿＿＿＿
- **参考答案**：① 顶点 = 元素；② 边 = 合并关系；③ 边权 = 无；④ 要求 = 判断两元素是否同集合。建图：并查集 合并/查询。

### P1262 间谍网络
- [ ] 考点：缩点 + 最小点权
- **我的四问**：＿＿＿＿
- **参考答案**：① 顶点 = 间谍；② 边 = 收买关系（A 收买 B 后可控制 B，有向）；③ 边权 = 点权（收买代价）；④ 要求 = 最少花费收买足够间谍。建图：Tarjan **缩点**，缩点后入度为 0 的强连通分量取最小收买价。

### P2746 校园网
- [ ] 考点：缩点（强连通）
- **我的四问**：＿＿＿＿
- **参考答案**：① 顶点 = 学校；② 边 = 信息传递（有向）；③ 边权 = 无；④ 要求 = ①至少给几个学校 ②加几条边变强连通。建图：**缩点**成 DAG，答案 = 入度 0 个数、max(入度0, 出度0)。

### P2002 消息扩散
- [ ] 考点：缩点（入度 0）
- **我的四问**：＿＿＿＿
- **参考答案**：① 顶点 = 节点；② 边 = 有向；③ 边权 = 无；④ 要求 = 最少从几个点出发能到所有点。建图：**缩点**后统计入度为 0 的个数。

---

## 第 6 步：建模技巧实战

### P1443 马的遍历
- [ ] 考点：网格 + 方向建图 + BFS
- **我的四问**：＿＿＿＿
- **参考答案**：① 顶点 = 每个棋盘格；② 边 = 马走 **8 个方向**的合法落点；③ 边权 = 步数 1；④ 要求 = 起点到各格最少步数。建图：预计算 8 方向偏移，BFS。

### P4554 小明的游戏
- [ ] 考点：0-1 BFS
- **我的四问**：＿＿＿＿
- **参考答案**：① 顶点 = 每个格子；② 边 = 相邻格子；③ 边权 = 0（字符相同）/1（不同）；④ 要求 = 起点到终点最少"切换字符次数"。建图：**0-1 BFS**（deque 优化）。

### P4568 飞行路线
- [ ] 考点：分层图 / 拆点（难点）
- **我的四问**：＿＿＿＿
- **参考答案**：① 顶点 = `(城市, 已用免费次数 k)` 状态；② 边 = 普通边（层内，权=路程）+ 免费边（第 k 层 → 第 k+1 层，权=0）；③ 边权 = 路程；④ 要求 = 最多用 k 次免费的最短路。建图：**拆点**成 `n*(k+1)` 个状态点，跑最短路。

### P1073 最优贸易
- [ ] 考点：状态拆点 / 正反最短路
- **我的四问**：＿＿＿＿
- **参考答案**：① 顶点 = `(城市, 状态)`（未买/已买/已卖）；② 边 = 城市间路径 + 状态转移（买、卖）；③ 边权 = 买卖价差；④ 要求 = 最大（卖出价 − 买入价）。建图：拆成 3 层状态求最长路，或**正向求最小买入、反向求最大卖出**再相减。

---

## ✅ 进度

> 每做完一题，把上方 `- [ ]` 改 `- [x]`，并把 `.cpp` 放进对应 `洛谷/` 分类文件夹。
