# 洛谷刷题仓库 · 操作指南

> 本仓库的一切日常操作都在这里。加题、提交、CI 自动化的流程，照着做就行。
> **本文件由 Agent 维护**：每次我（DSH Agent）教你新的操作性内容，都会自动追加到这里。如果你发现文档和实际操作不符，直接说「更新操作指南」。

## 一、新增一道题的完整流程（3 步）

1. **放代码**：把 `.cpp` 文件放进对应知识点文件夹
   ```
   dfs/          # DFS 相关
   bfs/          # BFS 相关
   dp/           # 动态规划相关
   并查集/        # 并查集相关
   ```
   文件命名规范：`题号 + 空格 + 题名.cpp`，例如 `P1037 [NOIP 2002 普及组] 产生数.cpp`

2. **（本地可选）刷新 README**：
   ```bash
   node gen-readme.js
   ```
   不跑也没关系——推到 GitHub 后 CI 会自动帮你刷新。

3. **提交推送**：
   ```bash
   git add .
   git commit -m "新增 P1037 题解"
   git push
   ```

## 一·五、dp 题单的使用方式（做 DP 题时）

仓库根目录有 **`dp题单.md`**（DP 刷题待完成计划，13 题分 4 关）。每做完一题：

1. **题解放进 `dp/` 文件夹**（命名 `P题号 题名.cpp`）
2. **打开 `dp题单.md`**，把对应项 `- [ ]` 改成 `- [x]`
3. **跑生成器**（或直接 push，CI 自动更新）：
   ```bash
   node gen-readme.js
   ```
4. 提交推送（同上）

> 题单里带进度统计（已完成 x/13）和建议路线（P1216 → P1115 → P1002 → P1048 → P1049 → P1164）。

## 二、新建一个知识点分类（比如 贪心/）

1. 创建文件夹并放入代码：
   ```bash
   mkdir 贪心
   # 把 Pxxxx 贪心题.cpp 放进去
   ```
2. **更新生成器的知识点映射**（可选但推荐）：打开 `gen-readme.js` 顶部 `CAT_LABEL`，加一行
   ```js
   const CAT_LABEL = {
     dfs: 'DFS',
     bfs: 'BFS',
     贪心: '贪心',   // ← 新分类加这里
     // ...
   };
   ```
   > 不加也能工作（会显示目录原名），加了显示更规范。
3. 跑一次生成器或直接 push：
   ```bash
   node gen-readme.js   # 仓库结构和题解列表都会自动更新
   git add . && git commit -m "新增贪心分类" && git push
   ```

## 三、CI 自动做了什么

| Workflow | 触发时机 | 作用 |
| --- | --- | --- |
| `c-cpp.yml` | 每次 push / PR 到 main | 递归编译检查所有 `.cpp`，绿勾 = 全部通过 |
| `update-readme.yml` | push 涉及 `.cpp` / `gen-readme.js` | 自动重跑生成器，若有变化自动提交新的 README |

> 自动提交使用 `[skip ci]` 标记，不会再次触发 CI，不会死循环。

## 四、README 生成器（gen-readme.js）

`gen-readme.js` 自动生成 README 的两个章节，**都不要手动编辑**：

| 章节 | 生成内容 |
| --- | --- |
| **仓库结构** | 扫描实际文件夹 → 列出各分类目录 + 自动标注题数（`dfs/ 2题`） |
| **题解列表** | 解析每个 `.cpp` → 生成题目表格（题号/题名/知识点/文件链接） |

特性：
- 同一题多个版本（如 P2097 的 BFS 版 + 并查集版）自动合并为一行
- 新增/删除文件夹或 `.cpp`，重跑一次自动同步（幂等，不会重复叠加）
- 知识目录名 → 显示名映射在脚本顶部 `CAT_LABEL`（见第二节）

## 五、常用操作速查

```bash
# 看仓库状态
git status

# 暂存 + 提交 + 推送（一条龙）
git add . && git commit -m "说明" && git push

# 只刷新 README 不提交
node gen-readme.js

# 本地编译一道题（以 P1162 为例）
g++ -std=c++17 -O2 "dfs/P1162 填涂颜色.cpp" -o main && ./main

# 拉取最新
git pull
```

## 六、注意事项

- `.cph/`（CPH 插件缓存）和编译产物已加入 `.gitignore`，不会被提交
- 文件名的 `题号` 部分必须是 `P` + 数字（如 `P1037`），生成器靠它识别和排序
- 不要手改 README 中「仓库结构」和「题解列表」，会被生成器覆盖；要改就改生成器或代码文件
- 分类文件夹名建议用中文或简短英文（dfs/bfs/dp/贪心/并查集），生成器都能识别

## 七、板子（算法模板）使用

仓库根目录的 **`板子/`** 是一套可复用的 C++17 算法模板，按知识点分成 8 个分类文件夹（基础/数学/数据结构/图论/字符串/搜索/DP/杂项），索引见 `[板子/README.md](板子/README.md)`。

- **每个 `.cpp` 都是可独立编译的完整程序**：上半部分是模板（函数/类/常量），下半部分 `main()` 是小演示，直接编译运行即可看到效果。
- **写题时**：把对应函数/类片段复制进题解即可，**不要**复制演示 `main()`。
- **本地验证**：
  ```bash
  g++ -std=c++17 -O2 "板子/数据结构/线段树.cpp" -o main && ./main
  ```
- **想扩充**：在对应分类文件夹里新增 `.cpp` 即可。`gen-readme.js` 已跳过 `板子/`，不会污染题解列表；CI 会照常编译这些模板文件（它们带演示 `main()`，能通过）。
- 遇到新学的算法，可以把该算法的板子加进来，作为个人的「考场速查库」。

> 🖨️ **打印整套板子**：运行 `node gen-templates-html.js`，会生成 `板子/模板合集.html`（语法高亮 + A4 分页）。浏览器打开后 `Ctrl+P` 即可打印/导出 PDF。**以后往 `板子/` 加了新 `.cpp`，重跑这条命令文档就自动更新，不用手改。**
